package org.models.core.enums;

public enum VehicleStatus {
    UNSOLD("UNSOLD"),SOLD("SOLD"),IN_PROCESS("INPROCESS"),ACCEPTED("ACCEPTED"),REJECTED("REJECTED");
    VehicleStatus(String rejected) {
    }
}
